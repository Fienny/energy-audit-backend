services:
  backend:
    build: ./backend
    container_name: energy-audit-backend
    restart: always
    ports:
      - "8000:8000"
    env_file:
      - ./backend/.env
    volumes:
      - backend-data:/backend/data

volumes:
  backend-data:
